extends layout

block content
	h1= title
	p Welcome to #{title}
		
	ul
		li.first
			a(href='#') foo
		li
			a(href='#') bar
		li.last
			a(href='#') baz


	ul
		li.first: a(href='#') foo
		li: a(href='#') bar
		li.last: a(href='#') baz		

	p
		| foo bar baz
		| rawr rawr
		| super cool
		| go jade go
	div#user #{name} &lt;#{email}&gt;

	- var html = "<script></script>"
	| !{html}

	label
	| Username:
	input(name='user[name]')

	//用户名
	label Username:
	input(name='user[name]')

	//if IE
		a(href='http://www.mozilla.com/en-US/firefox/') Get Firefox

	friends = 1
	case friends
		when 0
			p you have no friends
		when 1
			p you have a friend
		default
			p you have #{friends} friends	

	<h1>Title</h1>
	<p>foo bar baz</p>
	
	:markdown
		Woah! jade _and_ markdown, very **cool**
		we can even link to [stuff](http://google.com)
		```java
			 public void doDecorator(DecoratorContext context, Map result) {
		        try {
		            if (result == null) {
		                return;
		            }
		            List<FaqCategoryResponse> categoryIndex = (List<FaqCategoryResponse>) result.get("categoryIndex");
		            if(categoryIndex == null){
		                return ;
		            }
		            if(categoryIndex.size() <= 0){
		                return ;
		            }
		            FaqCategoryResponse lastCategory = categoryIndex.get(categoryIndex.size() - 1);
		            if(lastCategory == null || lastCategory.getTitle() == null){
		                return ;
		            }
		            if(lastCategory.getTitle().matches("^/.*$")){
		                categoryIndex.remove(lastCategory);
		            }
		        }catch (Exception e){
		            logger.error(e.getMessage() , e);
		        }
		    }
		```

	script.
		function a(){
			alert('a')
		}
